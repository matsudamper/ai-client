# Paparazzi

## UI 変更時
- `@Preview` を追加/更新しスナップショットを撮影する
- 画像なしで UI 作業完了にしない

## 撮影した画像をコミットするか
画像 1 枚ごとに、その画像がリポジトリに既にコミットされているかで判断する。

- 既にコミットされている画像は、撮り直した結果でコミットする
- まだコミットされていない画像は、コミットしない。PR とチャットに貼る

コミット済みの画像は一部のプレビュー分しか無い。
足りていないことを理由に、新しい画像を追加でコミットしてはいけない。
